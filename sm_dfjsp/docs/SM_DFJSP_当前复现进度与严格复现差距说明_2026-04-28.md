# SM-DFJSP 当前复现进度与严格复现差距说明（更新版，2026-04-28）

## 1. 文档目标与结论
- 目标：基于当前仓库状态，更新论文《Shared manufacturing-based distributed flexible job shop scheduling with supply-demand matching》（C&IE 189, 2024, 109950）的复现进度评估。
- 结论：阶段 D（同构性审计与可追溯交付）已完成；但阶段 A-C 的关键项仍未闭环，因此当前仍不能声明“严格按论文完成复现”。

## 2. 本次更新范围
本次更新相对上一版，重点同步以下变化：
1. 已新增并落地：
  - `docs/MODEL_EQ_MAPPING.md`
  - `docs/ALGO_MAPPING.md`
  - `docs/REPRO_REPORT.md`
  - `tests/test_model_constraints.py`
  - `tests/test_algorithm_regression.py`
2. 已完成测试验证：
  - 命令：`$env:PYTHONPATH='src'; python -m unittest discover -s tests -p "test_*.py" -v`
  - 结果：`Ran 8 tests, OK`
3. EDA-TS 三项优先改造已完成：
  - `tabu_key` 改为 move 级键（N1/N2/N3）。
  - `LMLS/LMLM` 改为按邻域动作类型触发。
  - 增加 PMA/PMS/PMM 迭代 trace 审计输出（jsonl）。

## 3. 当前已完成部分（相对论文）

### 3.1 工程复现主干
- 已实现 SM-DFJSP 的实例结构、四层编码（UA/OS/OP/MS）、可行性修复、解码评估。
- 已实现 EDA-TS 主体：PMA/PMS/PMM、多种群、TS 三邻域、非支配记忆池。
- 已实现对比算法入口：EDA、NSGA-II、EDA-VNS、H-GA-TS。
- 已实现指标链路：GD、IGD、C-metric、ODS、Wilcoxon（当前为正态近似）。

### 3.2 复现实验脚手架
- 已有 `MK01-15 -> SDMK01-15` 数据生成链路。
- 已有 `repro_mk01_15.py`（`quick/full`）与 repeated 管线脚本。
- 已有 `run_meta.json` 元数据记录机制。

### 3.3 阶段 D（已完成）
- 公式映射文档已完成：`docs/MODEL_EQ_MAPPING.md`。
- 算法映射文档已完成：`docs/ALGO_MAPPING.md`。
- 约束与行为回归测试已建立：`tests/`。
- 复现报告草案已完成：`docs/REPRO_REPORT.md`。

## 4. 当前未完成部分（严格复现关键缺口）

### 4.1 论文实例体系未覆盖
- 缺 `SDMK16-25`。
- 缺 `G01/G02/G03`。
- 影响：Table 5 与 25 实例全量对比/消融无法闭环。

### 4.2 论文口径 full 实验未闭环
- 当前结果主要为单实例 quick 口径（如 `sdmk05`，`n_runs=2`，`time_limit=8s`）。
- 尚未形成论文口径 full 产物闭环（全实例、重复次数、统一统计输出）。

### 4.3 Taguchi 口径不一致
- 论文参数实验实例为 `SDMK17`，当前默认流程仍基于 `sdmk15`。
- 当前数据目录无 `sdmk17.json`，无法直接按论文实例口径执行。

### 4.4 Gurobi 验证未闭环
- `run_gurobi_small.py` 仍只面向 `sdmk01-15`。
- 现有结果中存在 `gurobipy is not available`，Table 5 路线仍不可复现。

### 4.5 表格与图件交付仍不完整
- `build_paper_tables.py` 目前仅覆盖 Table 7/8/11/12/13。
- Table 5/9/10/14/15 仍需补充统一导出链路。
- `build_paper_figures.py` 与 repeated 指标文件接口仍需统一校准。

### 4.6 算法“近似复现”仍需收敛
- EDA-VNS/H-GA-TS 仍属于统一工程框架下的近似实现。
- TS 的 `tabu_key` 与 `LMLS/LMLM` 已完成工程升级（move 级 + 邻域触发），但仍未完成论文条款级逐项同构证明。
- PMA/PMS/PMM 已有 trace 审计输出，但尚未形成“论文步骤号级自动对拍报告”。

## 5. 复现进度判定（更新后）
- 工程可运行度：高。
- 可审计性（阶段 D）：中高（文档+测试已落地）。
- 可审计性（EDA-TS 细节）：中高（tabu/LMLS-LMLM/矩阵 trace 已增强）。
- 论文严格复现度：中低（实例体系与论文口径实验未闭环）。
- 判定：**阶段 D 已完成，但项目整体仍未达到“严格按论文复现完成”。**

## 6. 下一步执行优先级（更新后）

### P1（最高优先级）：阶段 A
1. 补齐 `SDMK16-25`。
2. 补齐 `G01/G02/G03` 数据与读取链路。
3. 统一脚本数据入口，支持多数据源目录。

### P2：阶段 B
1. 在 `SDMK17` 上执行 Taguchi `L25(5^6)`，每组合 `30` 次、每次 `100s`。
2. 跑全实例对比实验（多算法 + 30 次重复）。
3. 跑全实例消融实验（30 次重复）。

### P3：阶段 C
1. 完成 Table 9/10/13 的 Wilcoxon 自动汇编。
2. 增补 Table 5/14/15 自动导出脚本。
3. 统一图件生成链路与 repeated 指标文件格式。

## 7. 严格复现完成判定标准（保持不变）
- 覆盖 `SDMK01-25 + G01/G02/G03`。
- Taguchi 在 `SDMK17` 按 `30次×100s` 完成。
- Table 5/7/8/9/10/11/12/13/14/15 可一键重建。
- Gurobi 对照可运行并输出 gap。
- 具备 Eq./算法映射文档与可执行测试证据链。

## 8. 一句话结论（更新后）
- **当前项目已从“能跑”提升到“能跑且可审计（阶段 D 完成）”，但距离论文级严格复现仍差阶段 A-C 的关键闭环。**
